假设x的值为3，计算x的平方并赋值给y，分别以“y = x ∗ x”和“x ∗ x = y”的形式输出x和y的值。

输入格式：
本题无输入

输出格式：
按照下列格式输出代入x=3的结果：
y = x * x
x * x = y

Code(gcc):

#include <stdio.h>
int main(void)
{
    int x,y;
    x=3;
    y=x*x;
    printf("%d = %d * %d\n%d * %d = %d",y,x,x,x,x,y);
    return 0;
}
