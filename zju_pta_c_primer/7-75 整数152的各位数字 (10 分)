本题要求编写程序，输出整数152的个位数字、十位数字和百位数字的值。

输入格式：
本题无输入。

输出格式：
按照以下格式输出：
152 = 个位数字 + 十位数字*10 + 百位数字*100

Code(gcc):

#include <stdio.h>
int main(void)
{
    int b, s, g;
    b=1;
    s=5;
    g=2;
    printf("152 = %d + %d*10 + %d*100",g,s,b);
    return 0;
}
